import SignupForm from '@/components/home/SignupForm';
import React from 'react';

const page = () => {
    return (
        <div>
           <SignupForm switchToLogin={() => {}} /> 
        </div>
    );
};

export default page;